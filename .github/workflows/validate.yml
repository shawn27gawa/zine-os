name: Validate ZineOS

on:
  push:
    branches:
      - main
    paths:
      - "schema/**"
      - "templates/**"
      - "examples/**"
      - "scripts/validate_zine.py"
      - ".github/workflows/validate.yml"

  pull_request:
    paths:
      - "schema/**"
      - "templates/**"
      - "examples/**"
      - "scripts/validate_zine.py"
      - ".github/workflows/validate.yml"

  workflow_dispatch:

jobs:
  validate:
    name: Validate example publication
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install PyYAML jsonschema referencing

      - name: Validate basic ZineOS template
        run: |
          python scripts/validate_zine.py templates/basic/zine.yaml
